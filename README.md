# Watch My Escape

An LLM tries to escape a man-made puzzle room.

## Development

Use `uv` for dependency management:

```shell
uv sync
```

Run checks with Make:

```shell
make test
make lint
```

Run the app:

```shell
make dev
```
